# ==============================================================================
# SPCloud - Environment Variables Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# PostgreSQL Database Configuration
# ------------------------------------------------------------------------------
POSTGRES_USER=user
POSTGRES_PASSWORD=secret
POSTGRES_DB=mydb
POSTGRES_PORT=5434

# Database URL - używany przez backend (asyncpg dla async PostgreSQL)
# Format: postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql+asyncpg://user:secret@db:5432/mydb

# ------------------------------------------------------------------------------
# JWT & Security Configuration
# ------------------------------------------------------------------------------
# Klucz tajny do podpisywania tokenów
SECRET_KEY=your_secret_key

# Czas wygaśnięcia tokenu dostępu (w minutach)
JWT_EXPIRE_MIN=15

# Czas wygaśnięcia tokenu odświeżania (w dniach)
JWT_REFRESH_EXPIRE_DAYS=7

# Issuer tokenu JWT
JWT_ISSUER=SPCloud

# Algorytm szyfrowania JWT
JWT_ALGORITHM=HS256

# ------------------------------------------------------------------------------
# MinIO S3 Storage Configuration
# ------------------------------------------------------------------------------
# Endpoint MinIO (bez http://)
MINIO_ENDPOINT=minio:9000

# Klucze dostępu MinIO
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=supersecret

# Czy używać HTTPS (true/false)
MINIO_SECURE=false

# Nazwa bucket'a dla plików
MINIO_BUCKET_NAME=spcloud-files

# ------------------------------------------------------------------------------
# Frontend Configuration
# ------------------------------------------------------------------------------
# URL do API backendu (używany przez frontend)
PUBLIC_API_URL=http://localhost:8000

# Port frontendu
FRONTEND_PORT=3000

# ------------------------------------------------------------------------------
# Backend Configuration
# ------------------------------------------------------------------------------
# Port backendu
BACKEND_PORT=8000

# Tryb debugowania (true/false)
DEBUG=false

# CORS Origins (oddzielone przecinkami)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ------------------------------------------------------------------------------
# Docker Configuration
# ------------------------------------------------------------------------------
# Nazwa projektu Docker Compose
COMPOSE_PROJECT_NAME=spcloud
